## 🛠️ Prerequisites

Before running the tests, make sure you have:

- **Node.js** (v14 or later) → https://nodejs.org
- **Git** → https://git-scm.com

---

## ▶️ Setup & Installation

Clone the repository:

```bash
git clone https://github.com/Mivics1/plural-health-demo.git
cd plural-health-demo
npm install
npx cypress open
//Headless Mode
npx cypress run
